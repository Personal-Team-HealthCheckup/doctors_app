export const baseURL = '__base_url__'; // add the base url here

// add the endpoints here like this
export const endpoints = {
  LOGIN: 'auth/login',
  GET_PRODUCTS: 'products',
  SINGLE_PRODUCTS: 'products/:productId',
  GET_CATEGORIES: 'products/get-categories',
};

export const salt = 'doctor_app_salt'; // add the salt here
